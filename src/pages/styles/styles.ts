import styled from "styled-components";

export const IndexContainer = styled.div`
    position: relative;
    display: flex;
    flex-direction: column;
    flex-flow: column;

    .fixed{
        position: fixed;
        top: 0;
        z-index: 100;
    }
`